package com.finocio.block08_oop.access_modifiers.examples;

public class AccessModifiers {

    public static void main(String[] args) {

        // Encapsulamiento ()

        // - Modificadores de acceso

        // public (accesible desde cualquier clase)
        // private (solo accesible dentro de la clase)
        // protected (accesible desde subclases y mismo paquete)
        // (default) (accesible solo en el mismo paquete)

        // getters (método que devuelve el valor de un atributo privado)
        // setters (método que modifica el valor de un atributo privado, y puede validar datos antes de asignar

    }
}
